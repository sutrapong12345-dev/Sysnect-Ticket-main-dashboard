// Optional public runtime config.
// Copy to config.js only when a deployment intentionally exposes public fallback endpoints.
// Do not put private credentials here. Anything in this file is visible in the browser.
window.SYSNECT_CONFIG = {
    API_BASE: '',
    N8N_DIRECT_URL: '',
    EXPORT_WEBHOOK: '',
};
